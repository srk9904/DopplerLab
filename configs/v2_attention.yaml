metrics:
  train_clips: 1000
  train_ratio: 0.8
  val_ratio: 0.1
  batch_size: 16
  lr: 3.0e-4
  epochs: 300
  save_every: 20
  val_every: 20
  seed: 42

model:
  name: "attn_2d"
  params:
    d_model: 128
    nhead: 4
    num_layers: 2
    dim_ff: 256
    dropout: 0.1

dataset:
  version: "v2"
